# Notes

How I set it up:
```bash
# create virtual env
python -m venv venv
# activate
source venv/bin/activate
# install package
python -m pip install ECPy
python -m pip install pysha3
# exit venv
deactivate
```